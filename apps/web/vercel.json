{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @climatenote/web build",
  "installCommand": "echo 'install handled by buildCommand at the workspace root'",
  "framework": "nextjs"
}
